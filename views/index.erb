<h1>Todos</h1>

<% if @todos.empty? %>
<p>No todos found.</p>
<% else %>
<ol class="todo-list">
<% @todos.each do |todo| %>
<li class="todo-item">
  <%= link_to_priority todo.priority %>
  <%= todo.content %>
  <% todo.tags.each do |tag| %>
  <%= link_to_tag tag %>
  <% end %>
  <%= edit_link(todo) %>
</li>
<% end %>
</ol>
<% end %>  
